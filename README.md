# gitlab-runner-install

GitLab Runner’ni **Ubuntu/Debian** serverga tez va to‘g‘ri o‘rnatish uchun mini skriptlar.

---

## ✅ Talablar

- Ubuntu / Debian (APT bor bo‘lishi kerak)
- Internet chiqishi
- `sudo` huquqi (root bo‘lishi kerak)

---

## 🚀 1-qator bilan o‘rnatish (tavsiya)

Quyidagi buyruq:
- GitLab Runner repo’ni qo‘shadi
- `gitlab-runner` paketini o‘rnatadi / yangilaydi
- serviceni yoqadi va ishga tushiradi

```bash
curl -fsSL https://raw.githubusercontent.com/Muxtorov98/gitlab-runner-install/main/install.sh | sudo bash
```
